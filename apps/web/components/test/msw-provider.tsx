'use client'

/**
 * MSW Provider - Client Component (Deferred)
 * Mock Service Worker setup is deferred until post-Sprint 0.
 * This component is kept as a placeholder for future use.
 */

export function MSWProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
